# mobile-ios-ux

## Goal
Ensure perfect mobile experience, especially on iOS.

## Rules
- Use safe-area-inset (env())
- Avoid bottom navigation overlap
- Use min 44x44 touch targets
- Fix 100vh issues (prefer dvh)
- Maintain proper spacing (no stacked UI)